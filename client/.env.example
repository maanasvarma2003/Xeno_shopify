# API Configuration
# Backend API URL - Update this for local development only
# For Vercel deployment: Leave this UNSET - the app uses relative paths (/api) in production
# For local development: Use http://localhost:3000/api
VITE_API_URL=http://localhost:3000/api
